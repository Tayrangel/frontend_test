/*import { toContain } from '@nuxt/test-utils'
import faker from 'faker';

const tweet = require('../plugins/mirage');

test('to contain name in tweet', () => {
  expect(tweet).toContain('{name}');
});*/